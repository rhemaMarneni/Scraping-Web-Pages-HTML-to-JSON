/**
 * Read this input CSV file
 */
export const CSV_INPUT_PATH: string = "inputs/companies.csv";

/**
 * Output JSON file, containing scraped and structured data from all the companies in
 * `companies.csv`, will live here.
 */
export const JSON_OUTPUT_PATH: string = "out/scraped.json";
